const state={room:'Комната',texture:'Матовый',light:'Без сложного света'};document.querySelectorAll('.choices').forEach(group=>group.addEventListener('click',e=>{if(e.target.tagName!=='BUTTON')return;group.querySelectorAll('button').forEach(x=>x.classList.remove('active'));e.target.classList.add('active');state[group.dataset.group]=e.target.textContent;document.querySelector('#summary').textContent=`${state.room} · ${state.texture} · ${state.light}`}));document.querySelector('#leadForm').addEventListener('submit',e=>{e.preventDefault();const t=document.querySelector('#toast');t.classList.add('show');setTimeout(()=>t.classList.remove('show'),3500);e.target.reset()});
